import styled from 'styled-components';

const Container = styled.div`margin-top: 5rem;
  text-align: center;

  span {
    background: var(--headline);
    border: none;
    border-radius: 0.25rem;
    bottom: 2rem;
    color: var(--card-headline);
    font-size: 1.5rem;
    font-weight: 600;
    padding: 0 1rem;
    position: fixed;
    right: 2rem;
  }
`;

export default Container;
